import pandas as pd


def transform_data(df: pd.DataFrame) -> pd.DataFrame:
    '''
    This transformation function should return a DataFrame that only has albums released from 2019 onwards
    :param df: The raw DataFrame from the extract function
    :return: A DataFrame that only has albums from 2019 onwards
    '''
    df_trans = df

    return df_trans
