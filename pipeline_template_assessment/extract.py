import pandas as pd


def extract_data_from_source(filepath: str) -> pd.DataFrame:
    '''
    This function should extract your data from a filepath and output a Pandas DataFrame for Transformation
    :param filepath: the absolute filepath of your raw data
    e.g. /Users/liamhartley/PycharmProjects/spotify_analysis/pipeline_template/data/rapcaviar_raw.csv
    :return: a Pandas DataFrame for further transformations
    '''

    raw_data = 'use Python to open the file and load it into a DataFrame'

    return raw_data